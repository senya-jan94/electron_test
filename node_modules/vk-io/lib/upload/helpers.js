"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.copyParams = exports.isStream = void 0;

var _stream = require("stream");

/**
 * Check object is stream
 *
 * @param {Object} source
 *
 * @return {boolean}
 */
const isStream = source => typeof source === 'object' && source instanceof _stream.Stream;
/**
 * Copies object params to new object
 *
 * @param {Object} params
 * @param {Array}  properties
 *
 * @return {Object}
 */


exports.isStream = isStream;

const copyParams = (params, properties) => {
  const copies = {};

  for (const property of properties) {
    if (property in params) {
      copies[property] = params[property];
    }
  }

  return copies;
};

exports.copyParams = copyParams;